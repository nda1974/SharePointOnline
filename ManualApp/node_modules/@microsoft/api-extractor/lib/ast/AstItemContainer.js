"use strict";
// Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT license.
// See LICENSE in the project root for license information.
var __extends = (this && this.__extends) || (function () {
    var extendStatics = Object.setPrototypeOf ||
        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
        function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
var AstItem_1 = require("./AstItem");
/**
  * This is an abstract base class for AstPackage, AstEnum, and AstStructuredType,
  * which all act as containers for other AstItem definitions.
  */
var AstItemContainer = (function (_super) {
    __extends(AstItemContainer, _super);
    function AstItemContainer(options) {
        var _this = _super.call(this, options) || this;
        _this._memberItems = new Map();
        return _this;
    }
    /**
     * Find a member in this namespace by name and return it if found.
     *
     * @param memberName - the name of the exported AstItem
     */
    AstItemContainer.prototype.getMemberItem = function (memberName) {
        return this._memberItems.get(memberName);
    };
    /**
     * Return a list of the child items for this container, sorted alphabetically.
     */
    AstItemContainer.prototype.getSortedMemberItems = function () {
        var astItems = [];
        this._memberItems.forEach(function (astItem) {
            astItems.push(astItem);
        });
        return astItems
            .sort(function (a, b) { return a.name.localeCompare(b.name); });
    };
    /**
     * @virtual
     */
    AstItemContainer.prototype.visitTypeReferencesForAstItem = function () {
        _super.prototype.visitTypeReferencesForAstItem.call(this);
        this._memberItems.forEach(function (astItem) {
            astItem.visitTypeReferencesForAstItem();
        });
    };
    /**
     * Add a child item to the container.
     */
    AstItemContainer.prototype.addMemberItem = function (astItem) {
        if (astItem.hasAnyIncompleteTypes()) {
            this.reportWarning(astItem.name + " has incomplete type information");
        }
        else {
            this.innerItems.push(astItem);
            this._memberItems.set(astItem.name, astItem);
            astItem.notifyAddedToContainer(this);
        }
    };
    return AstItemContainer;
}(AstItem_1.default));
exports.default = AstItemContainer;

//# sourceMappingURL=AstItemContainer.js.map
