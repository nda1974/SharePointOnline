// [ts-npm-lint] removed reference to '../../typings/tsd.d.ts'
/// <reference types="@ms/sp-tab-tasklib" />
/**
 * Tests that clicking a Command Set Extension Item adds a div to the html body.
 */
declare class CommandSet implements ITabTest {
    private _serverURL;
    private _listName;
    private _commandItems;
    constructor(serverURL: string);
    TestName(): string;
    TestOwner(): string;
    /**
     * These scripts are required for the tests to run.
     */
    TestIncludeScripts(): string[];
    TestSetup(): void;
    TestRun(): void;
    /**
     * Ensures that a div element was appended to the documents html body.
     *
     * @param commandItem - an item on the command bar that is registered by the customActions
     * URL parameter.
     * @param dataAutomationId - the query string that uniquely identifies the div that should
     * be added to the page after clicking the command item.
     */
    private _testCommandItem(commandItem, dataAutomationId);
}
declare var TabTest: any;
