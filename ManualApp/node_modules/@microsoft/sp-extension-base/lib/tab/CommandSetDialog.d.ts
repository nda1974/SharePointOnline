// [ts-npm-lint] removed reference to '../../typings/tsd.d.ts'
/// <reference types="@ms/sp-tab-tasklib" />
/**
 * Tests that Command Set Extensions functions correctly with dialogs.
 */
declare class CommandSetDialog implements ITabTest {
    private _serverURL;
    private _listName;
    private _commandItems;
    constructor(serverURL: string);
    TestName(): string;
    TestOwner(): string;
    /**
     * These scripts are required for the tests to run.
     */
    TestIncludeScripts(): string[];
    TestSetup(): void;
    TestRun(): void;
    /**
     * Ensures a dialog is present upon clicking the 'commandItem', verifies the text
     * in the dialog matches 'expectedCommandText', and lastly ensures the dialog from the
     * 'commandItem' can be dismissed.
     * @param commandItem - an item on the command bar that is registered by the customActions
     * URL parameter.
     * @param expectedCommandText - the text that is expected to appear in the dialog
     * when the commandItem is clicked.
     */
    private _testCommandItem(commandItem, expectedCommandText);
}
declare var TabTest: any;
